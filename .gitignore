# Environment
.env

# Data — raw and processed are large/private, never commit
data/

# Python
__pycache__/
*.py[cod]
*.pyo
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/

# Models — pkl, safetensors, xgb.json etc are binary/large
models/
*.pkl
*.pt
*.safetensors
*.bin
*.xgb.json

# Notebooks checkpoints
.ipynb_checkpoints/

# macOS
.DS_Store
Icon

# Docker volumes (local only)
pgdata/

# Logs
*.log
logs/
__pycache__/
*.py[cod]

# Cloud Scripts (Hidden from GitHub)
scripts/modal_inference.py
scripts/modal_train.py
scripts/modal_compute.py
scripts/production/modal_compute.py
.agents/

# DevOps automation scripts (contain credentials — NEVER commit)
*.exp
run_crawler.sh
archive/ops/run_crawler.sh

# Infrastructure reference — contains live credentials, keep local only
INFRASTRUCTURE.md

# Database dumps — production data, keep local only
archive/*.sql

# Local AI tooling artifacts (security-scan results, repo wikis)
.mimosa/
.qoder/

# Eval harness transients (caches / checkpoints / logs — derived, not source)
eval/.embedding_cache.json
eval/.reembed_checkpoint.json
eval/reembed.log

# Fireworks migration transients (provider query cache / backfill checkpoint /
# cost ledger — derived operational state; authoritative billing is the dashboard)
eval/.embedding_cache_fireworks.json
eval/.fireworks_backfill.json
eval/fireworks_cost_ledger.jsonl
